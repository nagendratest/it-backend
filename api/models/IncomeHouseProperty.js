var IncomeHouseProperty = {
  // Enforce model schema in the case of schemaless databases
  schema: true,

  attributes: {
	rentalproperty:{type:'string'}	
  }
};

module.exports = IncomeHouseProperty;